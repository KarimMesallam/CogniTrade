# Use the project's main requirements file
# To install all dependencies:
# pip install -r ../requirements.txt

# If you need only the API-specific dependencies:
fastapi==0.110.0
uvicorn==0.25.0
websockets==11.0.3
python-dotenv>=1.0.0
pydantic==1.10.7
python-binance>=1.0.17
numpy==1.23.5  # Must be installed before pandas
pandas==1.5.3 